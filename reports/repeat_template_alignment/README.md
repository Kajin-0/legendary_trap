# Repeat-template alignment review

This bounded pass uses existing timing and cached base.en evidence only. Repeated Chorus occurrences are represented in relative coordinates; donor occurrences are combined with a median consensus and a clamped affine warp. Strong direct lines are retained. Missing leading edges use `repeat_template_edge_reconstruction`; sparse outro events use bounded local ad-lib evidence or explicit estimates.

Only `you_missed_it` and the late `commin_long_ways` outro were promoted and rerendered.
